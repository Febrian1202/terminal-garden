---
title: "Catatan Pertamaku"
date: "Mar 01 16:12"
---

# Hello, World!

Welcome to my terminal.

Ini adalah paragraf biasa dari **Markdown**. Ini contoh kode sebaris: `pnpm run dev`.

Dan di bawah ini adalah blok kode dengan _Syntax Highlighting_:

```typescript
// Contoh fungsi untuk menghitung total belanja di POS Kios
function hitungTotal(harga: number, jumlah: number): number {
  const total = harga * jumlah;
  console.log(`Total belanja: Rp${total}`);
  return total;
}

hitungTotal(15000, 3);
```
